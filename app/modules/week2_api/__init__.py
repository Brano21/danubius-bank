"""Week 2 - REST API (A01 BOLA/BFLA, mass assignment, error leak).
LOCKED until WEEK>=2; content added at the Week 2 build step."""
from flask import Blueprint

bp = Blueprint("week2", __name__, url_prefix="/api/v1")

# Routes added at Week 2.
